# Plan 09-01: detecção multi-janela e guarda de startup

## Tarefas

1. Inspecionar todos os `window_handles` do Selenium durante a espera pelo login.
2. Trocar para a janela autenticada antes de recapturar e validar os cookies.
3. Adicionar mensagens de diagnóstico para múltiplas janelas.
4. Proteger o startup da GUI contra chamadas simultâneas do evento `pywebviewready` e do timer de fallback.
5. Adicionar teste determinístico com uma segunda janela autenticada.

## Verificação

- Testes de `BrowserLogin` e suíte completa.
- `compileall`, `node --check` e smoke launch da build macOS.
