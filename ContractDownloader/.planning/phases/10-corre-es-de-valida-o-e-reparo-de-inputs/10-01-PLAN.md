# Plan 10-01: validação acionável e reparos seguros

## Tarefas

1. Retornar a lista completa de linhas/POs afetados nos grupos de validação, mantendo mensagens resumidas para respostas grandes.
2. Renderizar os POs/linhas em um bloco expansível na etapa 2.
3. Tornar o handler `Fix` resiliente a exceções, exibir erro na interface e revalidar imediatamente após o reparo.
4. Fazer `repair_input_file` respeitar o mapeamento persistido de colunas.
5. Adicionar testes para reparo com input mapeado, retorno de afetados e contrato da GUI.

## Verificação

- Testes unitários de `AppAPI.validate_input_file` e `repair_input_file`.
- Testes Playwright mock/real sem erros JavaScript.
- `uv run pytest -q`, `compileall`, `node --check` e `git diff --check`.
- Regenerar e validar a build macOS.
